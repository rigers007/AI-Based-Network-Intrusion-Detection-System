name: Time shower

on: 
  schedule:
    - cron: '*/5 * * *'

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The current server time is $(date)"
