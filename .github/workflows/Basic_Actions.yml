name: Show the time of the server

on:
  schedule:
    - cron: '* * * * *'

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "The time of the server is $(date)"
